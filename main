import sys
import os
sys.path.append(os.path.dirname(os.path.abspath(__file__)))


from flask import Flask, redirect, url_for, session
from random import randint
#from database import get_next_question


quiz, prev_question = 0, 0

app = Flask(__name__)

@app.route('/')
def index():
    global quiz, prev_question
    num_quiz = randint(1,3)
    prev_question = 0
    quiz = 1


    return '<a href="">Iniciar el cuestionario!<a/>'
    #si tengo una respuesta diferente a 200, se esta llamando mal a la función


def test():
    return 'Aquí veremos el resultado'


def result():
    return 'Página para el cuestionario!'

def get_next_question():
    # Esta función la completas con lo que te pida Algoritmika
    return "Aquí irá la siguiente pregunta"

app.add_url_rule('/', 'index', index)
app.add_url_rule('/test', 'test', test)
app.add_url_rule('/result', 'result', result)

app.run()
